```mermaid
sequenceDiagram
    participant browser
    participant server

    Note right of browser: User writes a note and saves.

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    server-->>browser: 201 Created (note stored successfully)
    deactivate server

    Note right of browser: List of notes are updated without reloading the page.